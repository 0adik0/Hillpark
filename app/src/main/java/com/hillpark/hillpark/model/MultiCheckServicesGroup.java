package com.hillpark.hillpark.model;

import com.thoughtbot.expandablecheckrecyclerview.models.MultiCheckExpandableGroup;

import java.util.List;

public class MultiCheckServicesGroup extends MultiCheckExpandableGroup {

    public MultiCheckServicesGroup(String title, List items) {
        super(title, items);
    }
}

